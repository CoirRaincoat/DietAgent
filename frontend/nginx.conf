server {
    listen 8080;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;
    server_tokens off;
    absolute_redirect off;
    client_max_body_size 1m;

    proxy_http_version 1.1;
    proxy_connect_timeout 5s;
    proxy_read_timeout 300s;
    proxy_send_timeout 300s;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;

    location = /api {
        return 308 /api/;
    }

    # The trailing slash removes /api/ while preserving the query string.
    location /api/ {
        proxy_pass http://backend:8000/;
    }

    # Public OpenAI-compatible endpoint. Disable proxy buffering so each SSE
    # record is forwarded immediately after the backend emits it.
    location /v1/ {
        proxy_pass http://backend:8000;
        proxy_buffering off;
        proxy_cache off;
    }

    # Keep existing CLI clients and the FastAPI documentation working.
    # /demo itself remains a frontend route.
    location ~ ^/(health|chat|demo/profiles|docs|openapi\.json|redoc)(/|$) {
        proxy_pass http://backend:8000;
    }

    location /assets/ {
        try_files $uri =404;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
